#!/usr/bin/env bash
# VS Code extensions Installer
#
# usage: code --list-extensions | xargs -L 1 echo code --install-extension | clip
#

code --install-extension shivapoudel.wp-extension-pack
